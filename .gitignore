# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.egg-info/
.pytest_cache/
.venv/
.env

# Logs
logs/
*.log

#schedules
data/raw/schedules/

# Ignore only EnergyPlus simulation output folder
data/intermediate/simulation_outputs/

# --- Bulky per-house generated / input data (several houses would bloat the repo) ---
# Processed CSV time-series outputs (training data, sim/mpc results, E+ CSVs, ...)
data/processed/**/*.csv

# EnergyPlus input files (IDF geometry, EPW weather)
data/**/*.idf
data/**/*.epw

# EnergyPlus run-output directories (eplusout.*, meters, tables, sql, msgpack, ...)
data/processed/**/eplus/
data/processed/**/eplus_*/

# MLflow local tracking store
mlruns/
mlflow.db
